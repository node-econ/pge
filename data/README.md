# Derived / reference datasets

CSV and similar outputs produced by scripts in `../scripts/` are written here (for example `utilities/ice_bofa_oas_tidy.csv` from `fred_ice_bofa_oas_tidy.py`).

Do not commit third-party data if your license terms forbid it (e.g. ICE BofA series via FRED — follow FRED and ICE terms).
